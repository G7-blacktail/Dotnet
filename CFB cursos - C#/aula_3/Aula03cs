using System;

    class Aula03
    {
        static void Main()
        {
            // Tipos de variaveis
            int numero = 0;
            char caracter = 'I';
            float flutuante = 1.6f;

            byte b = 10; // 0 ~ 255

            // tipo não primitivo
            string nome = "Gustavo";

            var 
        }
    }